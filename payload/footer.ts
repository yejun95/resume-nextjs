import { version } from '../package.json';

import { FooterPayload } from '../types/footer';

const footer: FooterPayload = {
  version,
  github: 'https://github.com/yejun95',
};

export default footer;
